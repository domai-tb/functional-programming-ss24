(*

(2 points) Declare a higher-order function hof : (char * (float * bool)) -> ((char * bool) -> int) -> (float -> (int * int)) -> (int * int) which converts all non-function arguments to integers using the supplied functions and then returns the sum and product of all obtained integers as a tuple.

*)
let hof = (* Fill in here *)
;;
